interface:
  display_name: "Video to Slides"
  short_description: "Turn videos into evidence-backed editable decks"
  default_prompt: "Use $video-to-slides to turn this video into an evidence-backed editable PowerPoint."
policy:
  allow_implicit_invocation: true
